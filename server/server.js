import express from 'express'`nimport cors from 'cors'`nimport dotenv from 'dotenv'`n`ndotenv.config()`n`nconst app = express()`nconst PORT = process.env.PORT || 5000`n`napp.use(cors())`napp.use(express.json())`n`napp.get('/api/health', (req, res) => {`n  res.json({ status: 'OK', message: 'Server is running' })`n})`n`napp.get('/api/projects', (req, res) => {`n  res.json({ projects: [] })`n})`n`napp.post('/api/projects', (req, res) => {`n  const { name, components } = req.body`n  res.json({ success: true, message: 'Project saved successfully', project: { id: Date.now().toString(), name, components } })`n})`n`napp.listen(PORT, () => {`n  console.log(`Server running on http://localhost:${PORT}`)`n})